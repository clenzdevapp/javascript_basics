let anonymeFunktion = (a, b) => {
  let ergebnis = a + b;
  return ergebnis;
};

console.log(anonymeFunktion(2, 3));

// AUFGABE 8
// a) Baue die Funktion multiplikationBerechnen um in eine anonyme Funktion mit zwei Parametern. Rufe sie per Konsole auf.
// b) Was ist der Unterschied zwischen der Funktion und der anonymen Funktion?